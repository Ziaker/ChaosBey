/// <reference types="vite/client" />

// Injected by vite.config.ts via `define`, so telemetry/bug reports can
// record which build/commit produced a given match (GDD section 72/116).
declare const __APP_BUILD_VERSION__: string;
declare const __APP_COMMIT_HASH__: string | null;
